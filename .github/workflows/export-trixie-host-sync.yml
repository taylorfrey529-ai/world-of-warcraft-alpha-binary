name: Export Trixie Host Multiarch Sync

on:
  push:
    paths:
      - '.github/workflows/export-trixie-host-sync.yml'

jobs:
  export:
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    steps:
      - name: Download current Debian 13 amd64 counterparts and development pins
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p out
          docker run --rm -v "$PWD/out:/out" debian:trixie-slim bash -lc '
            set -euo pipefail
            apt-get update
            cd /out
            apt-get download \
              apt libapt-pkg7.0 \
              util-linux mount libblkid1 libmount1 libsmartcols1 libfdisk1 libuuid1 liblastlog2-2 \
              libcap2 libcap2-bin \
              systemd systemd-sysv libsystemd0 libsystemd-shared libudev1 udev libpam-systemd \
              default-libmysqlclient-dev libaom-dev libarmadillo-dev libavif-dev \
              libcurl4-openssl-dev libexpat1-dev libfreexl-dev libgdal-dev libgeotiff-dev \
              libhdf4-alt-dev libhdf5-dev libheif-dev libkml-dev libmariadb-dev \
              libmariadb-dev-compat libnetcdf-dev libpq-dev libproj-dev libpython3-dev \
              libpython3.13-dev libsm-dev libspatialite-dev libssh2-1-dev libssl-dev \
              libtesseract-dev libxt-dev python3-dev python3.13-dev uuid-dev
          '

      - name: Record immutable package manifest
        shell: bash
        run: |
          set -euo pipefail
          cd out
          find . -maxdepth 1 -type f -name '*.deb' -print0 | sort -z | xargs -0 sha256sum > SHA256SUMS
          for pkg in ./*.deb; do
            printf '%s\t%s\t%s\t%s\n' \
              "$(dpkg-deb -f "$pkg" Package)" \
              "$(dpkg-deb -f "$pkg" Version)" \
              "$(dpkg-deb -f "$pkg" Architecture)" \
              "$(basename "$pkg")"
          done | sort > PACKAGE-METADATA.tsv

      - name: Upload host sync artifact
        uses: actions/upload-artifact@v4
        with:
          name: trixie-host-multiarch-sync
          path: out/
          if-no-files-found: error
          compression-level: 0
